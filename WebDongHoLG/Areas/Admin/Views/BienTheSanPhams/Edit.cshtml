@model WebDongHoLG.Data.BienTheSanPham
@{
    ViewData["Title"] = "Chỉnh sửa biến thể";
    ViewData["FormAction"] = "Edit";       
    ViewData["SubmitText"] = "Lưu thay đổi";  
}

<div class="page-header mb-3">
    <h2 class="text-primary">
        <i class="ti ti-edit me-2"></i>Chỉnh sửa biến thể
    </h2>
    <div class="text-secondary mt-1">Cập nhật chi tiết cho SKU: <b class="font-monospace text-blue">@(Model?.MaSku)</b></div>
</div>

<partial name="_FormBienThe" model="Model" view-data="ViewData" />